{
  "name": "@leadarrow/extension",
  "version": "0.0.0",
  "private": true,
  "description": "LeadArrow Chrome extension (lightweight softphone) — Manifest V3",
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "dev": "node ./scripts/build.mjs",
    "lint": "echo \"(extension) static MV3 package — no lint\" && exit 0",
    "typecheck": "echo \"(extension) static MV3 package — no typecheck\" && exit 0",
    "test": "echo \"(extension) no tests yet\" && exit 0"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.1"
  }
}
