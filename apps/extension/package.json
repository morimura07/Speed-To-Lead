{
  "name": "@leadarrow/extension",
  "version": "0.0.0",
  "private": true,
  "description": "LeadArrow Chrome extension (lightweight softphone) — Manifest V3",
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "dev": "node ./scripts/build.mjs",
    "lint": "echo \"(extension) lint added with the Phase 4 build pipeline\" && exit 0",
    "typecheck": "echo \"(extension) typecheck added with the Phase 4 build pipeline\" && exit 0",
    "test": "echo \"(extension) no tests yet\" && exit 0"
  }
}
